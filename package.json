{
  "name": "dragonbytes-ctf",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site — no build needed'",
    "seed": "echo 'Deploy first, then GET /api/seed?secret=YOUR_SEED_SECRET'"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  }
}
